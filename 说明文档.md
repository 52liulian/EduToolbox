# 说明文档 · EduToolbox 教师工具箱

> 项目全生命周期唯一管理载体。固定三大模块：**项目规划 / 实施方案 / 进度记录**。
> 每次开工先读本文档对齐进度；需求、方案、进度发生变动即时同步更新。
> 配套文档：[AGENTS.md](./AGENTS.md)（AI 协作约定）、[DESIGN.md](./DESIGN.md)（设计规范）、[CLAUDE.md](./CLAUDE.md)（AI 工作指南）。

- 项目名称：EduToolbox 教师工具箱（杏坛网络工作室）
- 线上地址：https://www.edutoolbox.org.cn （GitHub Pages + `CNAME`，纯静态托管）
- 文档建立：2026-09-24（对已运行项目补建，非新项目初始化）
- 维护人：榴莲；协作工程师别名：Kou（寇豆码）

---

## 一、项目规划

### 1.1 项目定位

面向中小学教师的**纯前端教育工具站**，由两部分构成：

1. **外链工具导航**：收录 AI 备课、组卷题库、课件 PPT、公开课、学科资源、音视频处理等免费/freemium 教育工具（`data.js` 已登记真实 URL 236 条，宣传口径 1000+，占位 `#` 条目持续人工核验替换中）。
2. **自研小工具**：`tools/` 下 **85 个**浏览器本地运行工具（评语、点名、倒计时、抽题、奖状、座位表、姓名贴等），数据不上传、可离线、可打印。

### 1.2 目标与边界

- **做**：教师备课、授课、班级管理全流程的高频小工具；外链导航与检索；A4 打印产物；课堂大屏（投影）场景。
- **不做**：不引入后端/数据库/账号体系；不做需要服务端或第三方密钥的功能；不做构建工程化（保持零构建、`file://` 双击可跑）。
- **隐私红线**：自研工具所有数据仅在浏览器内处理（localStorage 必须 try/catch 兜底）。

### 1.3 技术规划（固定，不随意变更）

| 维度 | 选型 |
|---|---|
| 架构 | 原生 HTML + CSS + JS（IIFE 命名空间 `window.EduToolbox.*`），无框架、无打包器 |
| UI 基座 | Bootstrap 5.3.3（本地化于 `assets/vendor/bootstrap/`）+ 站点自定义 CSS 覆盖，视觉保持自有风格 |
| 路由 | http(s) 用 History API 干净 URL；`file://` 保留 hash；`404.html` + sessionStorage 还原纯静态托管深链接 |
| 工具挂载 | Shadow DOM 适配器组件化为主（84 个 `mount:"adapter"`），原生挂载 1 个（scoreboard `mount:true`）；iframe 仅 bundle 缺失时兜底 |
| 离线方案 | 第三方库/字体全部本地化到 `assets/vendor/`；`file://` 下用预打包 bundle 绕过 fetch CORS |
| 主题 | `body.dark` 深浅色 + 舞台 `data-theme` 六色令牌，localStorage 持久化 |
| 语言 | 仅中文；现代浏览器（Chrome/Edge/Firefox/Safari 最近两个稳定版），不支持 IE11 |

### 1.4 内容规划（data.js）

- **17 个一级分类**：公开课、录课直播、教学资源（含子分类）、教学管理、教师成长、教育辅助、学术科研、资源下载、素材资源、多媒体处理、音频处理、图像处理、视频处理、安全软件、思维与创作、实用办公工具、Office 插件。
- **子分类**：`教学资源(resource)` 下挂 语文 / 数学 / 英语 3 个子分类；子分类为可选结构，未定义的分类自动退化为普通分类。
- **自研工具 85 个**：完整清单以 `assets/js/data.js` 的 `selfTools` 数组与 `tools/` 目录为准（二者 slug 必须一一对应）。
- **教学资讯**：`articles[]` 维护。

### 1.5 里程碑规划

| 里程碑 | 内容 | 状态 |
|---|---|---|
| M1 导航站成型 | 分类/外链数据、首页、搜索、Hash 路由、深浅主题 | ✅ 已完成 |
| M2 自研工具体系 | 工具目录规范、iframe 嵌入、data.js 注册、tool-common 共享底座 | ✅ 已完成 |
| M3 Bootstrap 迁移 + 组件化挂载 | 整站 Bootstrap 5 基座；工具 Shadow DOM adapter 挂载；共享舞台工具栏 | ✅ 已完成 |
| M4 全量工具交付与质检 | 85 工具 100% 组件化；file:// bundle 预打包；12 套件回归 | ✅ 已完成 |
| M5 持续运营 | 外链死链核验替换、占位 URL 补全、新工具按需追加、打印/大屏体验打磨 | ⏳ 进行中 |

---

## 二、实施方案

### 2.1 目录结构（权威现状）

```
EduToolbox/
├── index.html                # SPA 入口（动态 <base> + 深链接还原脚本必须最先执行）
├── 404.html                  # 纯静态托管深链接兜底（sessionStorage 存路径后 replace /）
├── CNAME                     # 自定义域名 www.edutoolbox.org.cn
├── server.py                 # SPA fallback 开发服务器（python server.py [port]）
├── server.js                 # Node 版 SPA 服务器（备用）
├── start.ps1 / start.sh      # Windows / POSIX 启动脚本
├── README.md                 # 项目自述（快速上手）
├── 说明文档.md                # 本文档（规划/方案/进度）
├── AGENTS.md / DESIGN.md / CLAUDE.md
├── assets/
│   ├── css/                  # base → layout → components → pages → tools → print 固定加载顺序
│   │                         # 另：bootstrap-theme.css / tool-common.css / tool-mount-base.css
│   ├── js/                   # data/utils/theme/search/render/toolkit/tool-stage-toolbar/
│   │                         # tool-adapter/router/app + frame-bridge.js（工具 iframe 通信）
│   ├── vendor/               # 第三方本地化：bootstrap/ katex/ fonts/ 及各 *.min.js
│   └── img/
├── tools/<slug>/             # 一工具一目录：index.html（+ 同名 css/js 或单文件）
│                             # 组件化工具另含构建产物 <slug>.bundle.js
└── docs/                     # 规范与阶段报告（directory-spec.md 等）
```

> 注：`.gitignore` 当前忽略 `.trae/`、`docs/`、`.workbuddy/`，三者为本地工作资料；本文档体系为根级业务文档，随仓库主体保留。

### 2.2 核心机制

1. **路由与深链接**：`router.js` 统一解析；http 下干净 URL 直接接管，静态托管 404 时由 `404.html` 把原路径写入 `sessionStorage["edutoolbox-spa-redirect"]`，`index.html` 顶部脚本在资源加载前 `replaceState` 还原。两段内联脚本的执行顺序严禁改动。
2. **工具组件化挂载（adapter）**：`tool-adapter.js` 拉取工具 `index.html` 文本 → 解析为 Shadow DOM（CSS rebase 进影子树，禁裸 `<link>` 注入全局 head）→ 挂到独立层叠上下文的舞台容器；工具内 z-index 越不过 `.topnav`(z-index:100)。
3. **file:// 预打包**：`fetch()` 在 file:// 被 CORS 拒绝，由构建脚本为每个工具生成 `tools/<slug>/<slug>.bundle.js`（内联 html + 本地 css/js 文本，CSS 相对 `url()` 重基，`</` 转义 `<\/`）；adapter 用 `<script src>` 加载，与 http 走同一条 Shadow 链路。**改工具源码后必须重跑构建，`--check` 会报 STALE。**
4. **共享舞台工具栏**：`tool-stage-toolbar.js` 在 stage 首子元素注入 `.stage-toolbar`；全屏目标是舞台自身，CSS 写 `.stage:fullscreen`（不写 `html:fullscreen`）；库调用必须写全 `window.EduToolStageToolbar.init(...)`。
5. **iframe 兜底与高度**：仅 bundle 缺失时回退 iframe；高度经 `frame-bridge.js` + ResizeObserver 自适应，router 不写死高度。iframe 全屏形态由工具内 `.edutf-solo`（fixed inset:0，100vw/100vh）承接 `:fullscreen` 样式。
6. **主题**：站点全局 `data-theme` 六色令牌只挂站点层；工具业务配色挂自身容器（`main.stage[data-skin]` 等），**禁止工具写 body/documentElement 的 data-theme**。
7. **打印**：`assets/css/print.css` 隐藏站点外壳；工具 `@page` 由 adapter 提升为文档级规则；浏览器自带页眉页脚无法用 CSS 关闭（只能在打印对话框取消）。
8. **外链**：所有外链跳转经过 `withUtm()` 统一参数；favicon 走 `https://a.favicon.im/{domain}?larger=true`，失败显示 🔗。
9. **分类计数口径**：全站统一用 `EduToolbox.render.countTools(cat)`（父分类直挂 + 全部子分类）与 `countAllTools()`，禁止写 `cat.tools.length`；`cat.tools` 必须有数组兜底。

### 2.3 开发与预览

```powershell
# Windows（本机固定环境）
python server.py 18420      # SPA fallback，建议端口 18420+
powershell -ExecutionPolicy Bypass -File start.ps1
```

- 静态资源服务器无 fallback，深链接会误判，调试 SPA 必须用 `server.py`。
- 本机代理坑：curl/Edge 默认走 http_proxy（127.0.0.1 也 502）；探活用 `curl --noproxy '*'`，headless Edge 加 `--no-proxy-server`。
- 修改 `index.html`、`data.js`、`router.js` 后需硬刷新（Ctrl+Shift+R）；静态资源更新加 `?v=yyyyMMdd` 版本参数。

### 2.4 新增自研工具流程（强制顺序）

1. `tools/<slug>/` 建目录，写 `index.html`（单文件或同名 css/js 三件套；删除 navbar/header 等框选内容；高度由内容撑开，不出现内部滚动条）。
2. 单文件工具在 `</style>` 前追加「组件化挂载兜底」块（六色 data-theme 变量 + .container 宽度约束），范式参照 `tools/100neijiajian/index.html`。
3. 在 `assets/js/data.js` 的 `selfTools` 追加 `{id, slug, name, icon, desc, mount:"adapter", ...}`，slug 与目录名完全一致。
4. 重跑 bundle 构建脚本生成 `<slug>.bundle.js`，`--check` 确认无 STALE。
5. 质检：`node --check`（如抽出独立 JS）+ `.workbuddy/scripts/run_all_suites.py` 回归 + http/file 双场景人工核对。
6. 完成后更新本文档「进度记录」。

### 2.5 质检体系（.workbuddy/scripts/）

- `run_all_suites.py`：一键 12 套件回归（验收门槛，须 12/12 全绿）。
- 静态：`audit_tools.py`（slug↔目录一致性）、`audit_deep.py`（语法/DOM id 交叉引用/资源路径）。
- 运行时：`smoke_test.js`（jsdom 加载 + canvas/音频/vendor mock）。
- 几何：Edge headless CDP 脚本（audit_mount_all / layout_measure / verify_search_layout 等），量测挂载模式、高度、裁切、居中与基线零漂移（#main 宽 1240 / font-size 15px / `--primary` #3b6ef6）。
- bundle：`build_tool_bundles.py --check`、`check_bundle_syntax.py`、`check_file_mount_all.py`。

### 2.6 风险与对策

| 风险 | 对策 |
|---|---|
| 工具 CSS 泄漏全局（裸 link 注入 head） | adapter 一律 Shadow rebase；native 用 `injectScopedCss()` |
| 批量脚本误改/清空文件 | 先备份；注释剥离后再判定；幂等 + 花括号配对守卫；失败后 checkout 回 HEAD 按序重放，不在损坏文件上续跑 |
| 缓存导致改动不生效 | 硬刷新；版本号 query；server.py 已设 no-store |
| file:// 与 http 渲染不一致 | bundle 预打包；双场景截图逐像素比对 |
| data.js 单分类 tools 缺失致全站崩 | `cat.tools = Array.isArray(...) ? ... : []` 兜底 |
| 「另存网页」入库夹带扩展残留 | 入库前查体积与 doubao/cici/sonner 等关键词（clean_tupianqiege.py 范式） |

---

## 三、进度记录

> 标注精确时间节点、验收标准与落地结果。历史节点依据 `docs/` 阶段报告与 `.workbuddy/memory/` 日志补录。

### 2026-09-18 ✅ 56 工具整体体检修复 + P0 交付 + Bootstrap/组件化启动

- **56 工具审计修复**：修复 11 类致命缺陷（半角引号闭合、TDZ、HTML 缺按钮致启动崩、localStorage 无兜底等）。验收：深度静态审计 56/56、jsdom 冒烟 56/56、注册映射 56/56 一致，0 报错。产出 `docs/tools-audit-report.html`。
- **P0 六工具**：award-generator、dictation-chinese、dictation-english、mental-math、grade-analyzer、group-roles，三件套规范交付并注册 data.js。验收：74 工具静态审计 0 error。产出 `docs/tools-p0-report.html`。
- **Bootstrap 5 基座 + mount 方案 D**：引入本地 Bootstrap 5.3.3 与 `bootstrap-theme.css` 变量映射；确定工具 Shadow DOM 组件化挂载路线。产出 `docs/bootstrap-mount-delivery.html`。

### 2026-09-21 ✅ P1/P2 工具交付与布局修复

- 完成 P1/P2 批次工具开发（见 `docs/tools-p1-report.html`、`docs/tools-p2-report.html`）。
- 全宽/布局修复（`docs/width-fix-report.html`）；当日综合修复报告 `docs/fix-2026-09-21-report.html`。
- 验收标准：各批次报告 IS_PASS；SPA 服务器页面可达。

### 2026-09-22 ✅ 舞台工具栏与主题审计、倒计时诊断、两轮修复

- 倒计时舞台诊断（`docs/diag-countdown-stage-report.md`）；工具栏/主题审计（`docs/audit-toolbar-theme-report.md`）。
- 两轮修复：`docs/fix-2026-09-22-report.html`、`docs/fix-2026-09-22-round2-report.html`。
- 验收：审计问题清零，舞台工具栏与多主题在挂载态/独立页表现一致。

### 2026-09-23 ✅ 组件化全覆盖 + file:// bundle + 全屏统一 + 子分类 + 姓名贴

- **85 工具 100% 组件化**：http 84/84 adapter + 1 native；file:// 经 `build_tool_bundles.py` 预打包后同样 84/84 shadow 挂载、0 iframe 兜底，双场景渲染一致；删除 router 写死高度（innerHeight-150），`#toolIframe` min-height 360px 兜底。
- **全屏形态统一**：52 个工具 269 处 `:fullscreen` 选择器追加 `.edutf-solo` 并列变体；修复 `tool-stage-toolbar.js` 模块顶层引用 init 作用域变量的 ReferenceError；solo 尺寸改 100vw/100vh 消除滚动条缝隙。
- **高考倒计时**：舞台 min-height 100vh 铺满（挂载态 `:host main.stage{min-height:460px}` 收回），设置弹窗改屏幕居中。
- **分类体系**：全站计数统一 `countTools/countAllTools`；`/section`、`/search`、`/categories` 三页共用 `#categoryGridView` + `renderCategoryHeader()`；首页子分类自适应 tab 与深链 `?sub=` 落地（设计见 `.trae/documents/subcategory-tab.md`）。
- **姓名贴（xingmingtie）**：旧 name-sticker 重做为单文件整页工具，data.js 改 `mount:"adapter"`；修复 bundle 构建对 `href` 先于 `rel` 的 `<link>` 漏打包问题。
- **字体本地化**：Google Fonts 7 个字族（Long Cang/Ma Shan Zheng/ZCOOL QingKe HuangYou/ZCOOL XiaoWei/Zhi Mang Xing/Noto Sans SC/Noto Serif SC）662 个 woff2 子集（约 25MB）下载到 `assets/vendor/fonts/`，CSS URL 全部本地化，消除外部请求。
- 验收：`run_all_suites.py` 12/12 全绿；http/file 双场景 85 工具普查（模式+高度+裁切）通过；基线漂移 diff 仅改善项。

### 2026-09-24 ✅ 根级文档体系补建

- 补建 `说明文档.md / AGENTS.md / DESIGN.md / CLAUDE.md` 与 `.trae/rules/project_rules.md`。
- 同步 README 工具数 56→85、vendor/fonts 目录结构；`docs/directory-spec.md` 补 vendor 层。
- 验收标准：5 份文档齐全；文档中数字（85 工具、17 一级分类、84 adapter+1 native、236 条已登记外链）与代码实际一致。

### 2026-09-24 ✅ 前台页脚改版（四列信息架构 + 工作室版权）

- 按参考设计将全站共用页脚（`index.html` `.footer`，各路由共用）由左右两段式重做：**四列网格**（品牌介绍〔🎓 Logo+副标题“一站式教师工具资源导航平台”+原版权免责声明〕/ 快速链接〔首页·全部分类·必用工具·教学资讯〕/ 法律信息〔隐私政策·服务条款〕/ 联系我们〔技术支持〕）+ 分隔线下**居中版权栏**「© 2026 杏坛网络工作室 · 版权所有」。
- `assets/css/layout.css` 重写 footer 段：背景改 `--bg-soft`（浅 #f4f7fd / 深 #191e2a），`grid-template-columns: 1.5fr 1fr 1fr 1fr`，列标题 --text-1、链接 --text-2 hover 主色、静态项 --text-3；`assets/css/tools.css` ≤760px 断点改两列折行（品牌列跨整行）。
- 死链清理：移除原 `/about` 链接（路由不存在）；隐私政策/服务条款/技术支持对应页面未建，暂渲染为 `.footer-static` 静态文字（不可点、无死链），页面上线后改为链接。
- 验收：浏览器实测四列网格 341+227×3、5 个交互链接均真实可跳（/articles 点击验证通过）；深色模式五色令牌正确；无控制台错误；工具内自有 `.footer`（如 99table，Shadow DOM 内）不受影响。

### 当前进行中（M5）

- 外链占位 `#` 与死链持续核验替换；按教学需求追加新工具（遵守 2.4 流程）；打印与课堂大屏体验优化。
- 已知遗留：tupianqiege 曾引用不存在的「图片切割工具_files/」残留资源（单文件保存残留），后续清理。
